# push 到 main 分支时：在 GitHub 云端构建 backend/frontend 镜像 → 推送到 ghcr.io → SSH 到服务器拉取并重启
name: Build and Deploy

on:
  push:
    branches: [main, master]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image names
        id: images
        run: |
          OWNER="${{ github.repository_owner }}"
          OWNER_LC=$(echo "$OWNER" | tr '[:upper:]' '[:lower:]')
          echo "backend=$REGISTRY/$OWNER_LC/autoattend-backend:latest" >> $GITHUB_OUTPUT
          echo "frontend=$REGISTRY/$OWNER_LC/autoattend-frontend:latest" >> $GITHUB_OUTPUT

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./atuo_attend_backend
          push: true
          tags: ${{ steps.images.outputs.backend }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./auto_attend_forntend
          push: true
          tags: ${{ steps.images.outputs.frontend }}

      - name: Deploy to server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/AutoAttend' }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${SSH_PORT}" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true
          scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -P "${SSH_PORT}" -r \
            docker-compose.prod.yml \
            atuo_attend_backend \
            "${SSH_USER}@${SSH_HOST}:${DEPLOY_PATH}/"

      - name: Start containers on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 3h
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH || '/mnt/newdisk/app/AutoAttend' }}
            export IMAGE_BACKEND=${{ steps.images.outputs.backend }}
            export IMAGE_FRONTEND=${{ steps.images.outputs.frontend }}
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d
            # 团队管理表结构迁移：已有库补全 biz_user 头像/备注名/职务列（新库建表脚本已含，执行报错可忽略）
            sleep 5
            docker run --rm --network container:autoattend-mysql \
              -v "$(pwd)/atuo_attend_backend/src/main/resources/db/schema_collab_team_migration.sql:/migrate.sql:ro" \
              mysql:8.4 sh -c "mysql -h 127.0.0.1 -u autoattend -pautoattend_pwd autoattend < /migrate.sql" || true
