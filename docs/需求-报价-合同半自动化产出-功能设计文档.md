# 需求 → 报价计算器 → 合同 半自动化产出 — 功能设计文档

## 1. 文档说明

- **功能名称**：需求→报价计算器→合同半自动化产出（外包工作室报价与合同工具）
- **版本**：v0.1
- **目标**：在 AutoAttend 中新增**需求→报价→合同**的半自动化链路，使**管理员**能够通过**点选/结构化方式**快捷录入或关联需求，系统按**人天基准 + 风险系数**计算报价并生成**报价单**，再通过 **AI 自动生成合同**，减少 Excel/Word 手工报价与合同撰写时间（目标：单项目从 2–3 小时压缩到 15–30 分钟），同时通过**结构化需求 + 置信度 + 人工审核**保障报价准确性。
- **定位**：面向外包工作室的「生存工具」——项目启动前必做的报价与合同环节，标准化、可追溯、可学习。
- **依赖**：现有 AutoAttend 技术栈（Vue、Spring Boot、MySQL）；大模型 API（合同生成）；可选与「多维表格」需求记录联动；PDF 生成（服务端或前端库）。

---

## 2. 功能目标与范围

### 2.1 目标

- **需求录入**：管理员通过**结构化点选**（项目类型、技术栈、功能模块与功能点+复杂度）或**从多维表格需求记录一键带入**，快速形成可计算的需求清单；禁止纯自由文本作为唯一输入，降低模糊度。
- **报价计算**：基于**人天基准库**（按技术栈×复杂度）计算基础人天，再叠加**风险系数**（需求变更、技术风险、沟通成本、维护预留、紧急程度等）与**地域/市场单价**，输出报价金额与**置信度评分**；支持**人工调整**与**审核清单**确认后再生成报价单。
- **合同生成**：在报价确认后，将**报价摘要 + 关键条款**提交 AI，自动生成**合同正文**（可编辑），支持公司模板/水印，导出 Word 或 PDF。
- **准确性保障**：**三层机制**——（1）结构化需求拆解；（2）动态校准引擎（人天基准 + 风险系数 + 可选市场锚定）；（3）人工审核与反馈闭环（置信度、审核清单、实际成本回填以校准算法）。

### 2.2 范围（本期）

| 在范围内 | 不在范围内（可后续迭代） |
|----------|---------------------------|
| 管理员端：需求结构化录入（项目类型/技术栈/功能模块与功能点+复杂度） | 客户自助录入、多角色审批流 |
| 人天基准库（内置按技术栈×复杂度）、风险系数配置与计算 | 实时抓取外包平台价格、多地域实时单价 |
| 报价计算：基础人天 + 风险调整 + 单价 → 报价金额 | 多币种、多税率、复杂分成 |
| 置信度评分（基于需求完整度等）、报价审核清单（勾选确认） | 全自动报价无需人工确认 |
| 报价单生成（PDF/HTML，含公司信息与明细） | 在线电子签章、法律存证 |
| 基于报价单关键信息 + AI 生成合同正文（可编辑） | 合同多版本对比、法务审核流 |
| 可选：与多维表格「需求记录」关联，从表格带入需求摘要 | 从 Git/PR 自动识别需求 |
| 历史报价/合同归档、可选实际人天回填用于校准 | 多团队效率系数、市场价爬虫 |

---

## 3. 用户角色与场景

| 角色 | 操作 |
|------|------|
| **管理员** | 创建/编辑「报价项目」；通过结构化向导或从多维表带入需求；查看人天与报价计算结果、置信度与风险提示；调整参数、确认审核清单；生成报价单 PDF；触发 AI 生成合同并编辑、导出；查看历史报价与合同；可选录入实际人天用于校准。 |
| **系统** | 根据需求结构计算人天与报价；计算置信度并给出审核提示；调用大模型 API 生成合同正文；存储报价与合同版本。 |

---

## 4. 功能需求详述

### 4.1 需求录入（结构化）

- **原则**：以**结构化点选为主**，减少「客户说做个类似淘宝的页面」这类模糊输入导致的报价偏差。
- **项目基础**（必填/点选）：  
  - **项目类型**：企业官网、电商小程序、管理后台、APP、其他。  
  - **技术栈**：Vue+Node、React+Java、小程序原生、Flutter、其他。  
  - **设计需求**：已有设计稿、需 UI 设计、套用模板。  
  - **数据迁移**：无、需从旧系统迁移、需第三方接口对接。
- **功能模块与功能点**：  
  - 支持**多模块**（如用户系统、商品管理、订单系统）；每模块下**多条功能点**。  
  - 每条功能点：**名称** + **复杂度**（简单/标准/中等/复杂/极复杂）+ **数量**（默认 1）。  
  - 复杂度定义见 4.2；支持从预设库点选常用功能点（如「手机号登录」「微信 OAuth 登录」）快速添加。
- **非功能需求**（可选）：  
  - 并发用户量级：&lt;100、100–1000、1000–10000、10000+。  
  - 安全要求：普通、金融级、政府级。  
  - 部署方式：单机、云服务器、容器化/K8s。
- **与多维表格联动**（可选）：若启用，可从「多维表格」中选择某项目的需求记录，将**问题描述/归属模块**等摘要带入当前报价项目，再补充功能点与复杂度，避免重复录入。

### 4.2 复杂度与人天基准

- **复杂度定义**（内置行业基准）：

| 复杂度 | 定义 | 参考人天 | 示例 |
|--------|------|----------|------|
| 简单 | 纯展示，无交互，无后端 | 0.5–1 天 | 静态页、文章详情 |
| 标准 | 常规 CRUD，标准组件 | 1–2 天 | 用户注册、列表管理 |
| 中等 | 需定制开发，有业务逻辑 | 2–4 天 | 支付流程、权限系统 |
| 复杂 | 算法/性能/第三方对接 | 4–8 天 | 推荐算法、高并发优化 |
| 极复杂 | 创新功能，技术预研 | 8 天+ | 实时音视频、区块链 |

- **人天基准库**：按**技术栈 × 复杂度**存储参考人天（可配置），例如：

| 技术栈 | 简单 | 标准 | 中等 | 复杂 | 极复杂 |
|--------|------|------|------|------|--------|
| Vue+Node | 0.5 | 1.5 | 3 | 6 | 10 |
| React+Java | 0.6 | 1.8 | 3.5 | 7 | 12 |
| 小程序 | 0.4 | 1.2 | 2.5 | 5 | 8 |
| Flutter | 0.7 | 2 | 4 | 8 | 15 |

- **计算**：各功能点人天 = 基准人天 × 数量；**项目总人天** = Σ 功能点人天（可再乘以团队效率系数，见 4.5）。

### 4.3 风险系数与报价计算

- **风险系数**（可配置，百分比叠加）：  
  - **需求变更风险**：+10%～+20%（如首次合作、无明确 PRD、传统行业客户）。  
  - **技术风险**：+10%～+15%（新技术栈、需预研）。  
  - **沟通成本**：+5%～+10%（异地、时差、非技术背景客户）。  
  - **维护预留**：+10%～+15%（含 1 年维护、无明确验收标准）。  
  - **紧急程度**：-10%（标准周期）或 +20%～+30%（加急）。
- **单价**：支持按**地域/市场**配置人天单价（如一线 ¥1500–2500/天，新一线 ¥1000–1800/天），或固定单价。
- **公式**：  
  - **基础报价** = 总人天 × 人天单价。  
  - **风险调整** = 基础报价 × (1 + Σ 风险系数)。  
  - **最终报价** = 基础报价 + 风险调整（或合并为一口价展示）。

### 4.4 置信度与人工审核

- **置信度评分**（0–100）：  
  - 依据：需求是否含详细 PRD、功能点数量与粒度、是否存在「类似 XX」等模糊参照、是否有历史相似项目可参考、复杂度跨度等。  
  - **高（90–100）**：可直接生成报价，提示「基于充分信息」。  
  - **中（70–89）**：生成报价，标注「建议人工复核以下 X 点」。  
  - **低（50–69）**：生成区间报价（如 ±30%），建议人工确认。  
  - **极低（&lt;50）**：建议先补充信息再生成。
- **报价审核清单**（生成报价单前可强制勾选）：  
  - 是否限制需求变更次数（如 ≤3 次）；  
  - 是否明确验收标准（功能清单/测试用例）；  
  - 是否约定付款节点（如 3-4-3、5-5）；  
  - 是否包含部署上线费用；  
  - 是否明确维护期及范围。  
  未勾选时可提醒，但不阻断生成。

### 4.5 报价单与合同生成

- **报价单**：  
  - 内容：项目名称、需求摘要、功能模块与功能点明细（含人天）、风险说明、总人天、单价、总价、付款建议、公司信息与 Logo。  
  - 输出：PDF 或 HTML，支持公司水印/品牌。
- **合同生成**：  
  - **输入**：报价单中的项目名称、总价、付款节点、交付范围摘要、维护期等关键信息 + 公司名称、客户名称（管理员填写）等。  
  - **AI**：将上述结构化信息与合同模板要求（如「软件开发合同」「含保密与知识产权」）组装为 prompt，调用大模型生成**合同正文**。  
  - **输出**：富文本/Word 可编辑，管理员可修改后再导出 PDF/Word。  
  - **模板**：支持预设「软件开发合同」「维护服务合同」等模板类型，在 prompt 中指定。

### 4.6 实际成本反馈与校准（可选）

- **项目结束后**：管理员可录入**实际人天**、偏差原因（如需求变更、技术难点）。  
- **系统**：  
  - 记录「预估人天 vs 实际人天」，用于后续**团队效率系数**（实际/预估均值）校准。  
  - 可选：对「含某类功能点」或「某客户类型」的项目自动建议风险系数微调。  
- **本期**：可仅落库实际人天与原因，校准算法为后续迭代。

---

## 5. 数据模型建议

### 5.1 报价项目与需求

| 表名 | 说明 |
|------|------|
| `biz_quote_project` | id、name、project_type、tech_stack、design_type、data_migration、concurrency、security_level、deploy_type、status、created_at、updated_at；可选 link_table_id 关联多维表项目。 |
| `biz_quote_module` | id、quote_project_id、name、sort_order。 |
| `biz_quote_item` | id、module_id、name、complexity、quantity、estimated_days、sort_order。 |

### 5.2 基准与配置

| 表名 | 说明 |
|------|------|
| `biz_quote_baseline` | id、tech_stack、complexity、days；人天基准库，可按技术栈+复杂度查询。 |
| `biz_quote_risk_config` | id、risk_key、label、default_pct、enabled；风险系数配置。 |
| `biz_quote_price_config` | id、region_label、price_per_day、currency；地域/单价配置。 |

### 5.3 报价结果与合同

| 表名 | 说明 |
|------|------|
| `biz_quote_result` | id、quote_project_id、total_days、base_amount、risk_pct、risk_amount、final_amount、confidence_score、audit_checklist_json、created_at。 |
| `biz_quote_document` | id、quote_result_id、type（quote/contract）、file_path_or_content、version、created_at。 |
| `biz_quote_contract_draft` | id、quote_result_id、client_name、company_name、ai_prompt_snapshot、ai_raw_response、edited_content、created_at、updated_at。 |

### 5.4 实际成本反馈（可选）

| 表名 | 说明 |
|------|------|
| `biz_quote_actual` | id、quote_project_id、estimated_days、actual_days、deviation_reason、created_at。 |

---

## 6. 接口与流程建议

### 6.1 需求与报价

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/quote/projects | 创建报价项目。 |
| GET  | /api/admin/quote/projects | 报价项目列表（分页、筛选）。 |
| GET  | /api/admin/quote/projects/{id} | 报价项目详情（含模块与功能点）。 |
| PUT  | /api/admin/quote/projects/{id} | 更新项目基础与模块/功能点。 |
| POST | /api/admin/quote/projects/{id}/calculate | 计算人天与报价，返回总人天、基础报价、风险调整、最终报价、置信度、风险提示。 |
| POST | /api/admin/quote/projects/{id}/quote-doc | 生成报价单（返回 PDF 或 HTML 流/URL）。 |
| GET  | /api/admin/quote/baselines | 人天基准列表（按技术栈）。 |
| GET  | /api/admin/quote/risk-config | 风险系数配置。 |
| GET  | /api/admin/quote/price-config | 单价配置。 |

### 6.2 合同

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/quote/results/{id}/contract/generate | 提交报价摘要+客户/公司信息，调用 AI 生成合同正文，落库并返回。 |
| GET  | /api/admin/quote/results/{id}/contract | 获取合同草稿（含 AI 原文与编辑后内容）。 |
| PUT  | /api/admin/quote/results/{id}/contract | 保存编辑后合同内容。 |
| POST | /api/admin/quote/results/{id}/contract/export | 导出合同为 Word/PDF。 |

### 6.3 与多维表联动（可选）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | /api/admin/quote/projects/{id}/link-table-requirements | 可选，从关联多维表拉取需求记录摘要，供「带入需求」使用。 |

---

## 7. 前端流程与界面建议

- **流程**：**选择/创建报价项目** → **结构化需求录入**（项目基础 + 功能模块与功能点）→ **计算报价**（展示人天、金额、置信度、风险提示）→ **审核清单勾选** → **生成报价单** → **填写客户/公司信息** → **AI 生成合同** → **编辑合同** → **导出**。
- **需求录入页**：  
  - 项目类型/技术栈/设计/数据迁移等下拉或单选。  
  - 功能模块列表，每模块下功能点列表；功能点支持从「预设库」点选（名称+复杂度）或手动输入。  
  - 实时或按需计算并展示「当前预估人天」与「置信度」简要提示。  
- **报价结果页**：  
  - 展示总人天、基础报价、各项风险系数与调整后金额、最终报价。  
  - 置信度等级与「建议复核」项。  
  - 审核清单（复选框），通过后允许生成报价单。  
- **合同页**：  
  - 表单：客户名称、公司名称、合同类型等。  
  - 一键「AI 生成」后展示富文本，可编辑。  
  - 导出 Word/PDF。

---

## 8. 非功能要点

- **安全**：报价与合同仅管理员可访问；AI 调用 Key 仅后端持有；合同与报价单可做访问日志。  
- **可追溯**：报价结果与合同版本落库，支持按项目查看历史。  
- **可配置**：人天基准、风险系数、单价、审核清单项均可配置，便于不同团队校准。

---

## 9. 与现有 AutoAttend 能力的关系

- **多维表格**：可选将「报价项目」与某项目多维表关联，从表格需求记录带入摘要，减少重复录入；多维表不依赖本功能。  
- **单次提交 AI 分析**：独立能力；若后续需要「根据 commit 关联需求并更新多维表状态」，可参考《单次提交AI分析-功能设计文档》第 10 节与多维表联动。  
- **管理员后台**：本功能入口放在管理员侧，与现有 commit 看板、多维表入口并列。

---

## 10. MVP 迭代建议

- **第一阶段**：项目类型/技术栈/功能模块与功能点（含复杂度）、人天基准表、基础人天计算、人天单价配置、报价金额计算、简单置信度（如按功能点数量与 PRD 是否填写）、报价单 PDF 生成、公司信息配置。  
- **第二阶段**：风险系数配置与计算、报价审核清单、AI 合同生成（单模板）、合同编辑与导出。  
- **第三阶段**：与多维表需求带入、预设功能点库、实际人天回填与效率系数校准。  
- **第四阶段**：多合同模板、市场价区间提示（若接外部数据）、多角色与审批流（若需要）。

---

## 附录 A：报价准确性保障三层小结

| 层级 | 要点 |
|------|------|
| **第一层：结构化需求** | 强制点选项目类型/技术栈/功能点+复杂度，禁止仅自由文本；降低「需求模糊」导致的报价偏差。 |
| **第二层：动态校准** | 人天基准库（技术栈×复杂度）+ 风险系数 + 地域单价；可选历史实际人天反馈校准团队效率系数。 |
| **第三层：人工兜底** | 置信度评分 + 报价审核清单 + 最终报价与合同由管理员确认后生成；项目结束后可选录入实际人天。 |

**原则**：结构化输入 &gt; 智能算法 &gt; 人工兜底；第一层做不好，后续易「垃圾进垃圾出」。报价计算器的价值在于把隐性经验显性化、把遗漏风险提前暴露，**最终报价仍由人确认**，决策质量提升。

---

**文档结束。**
