# 报价系统 v1 — 实施任务清单（与 `报价系统-v1-解决方案报价与工作区模式-功能设计文档` 对齐）

本文档用于跟踪 v1 能力落地情况；完成后可作为迭代回顾依据。

## Phase A — 解决方案级多交付物

| 任务 | 状态 | 说明 |
|------|------|------|
| 数据模型：`biz_quote_module.deliverable_key` / `deliverable_label` | 完成 | 迁移 `schema_quote_deliverable_workspace_migration.sql` |
| 保存/加载 API 与前端表单 | 完成 | `QuoteModuleSaveDto`、报价编辑页模块行 |
| AI 多交付物解析 | 完成 | `multiDeliverableMode` + `deliverables[]` JSON，失败时回退 `modules[]` |
| 报价单 HTML/PDF：分交付物列与小计 | 完成 | `renderQuoteHtml` |

## Phase B — 基线价与商务等比例调价

| 任务 | 状态 | 说明 |
|------|------|------|
| `biz_quote_item`：行基线/调价后金额、`excluded_from_scale` | 完成 | 计算后按人天权重分摊基线总价 |
| `biz_quote_result`：`baseline_final_amount`、`price_scale_factor`、`adjusted_final_amount` | 完成 | `POST /api/admin/quote/projects/{id}/price-adjust` |
| 前端：目标总价/系数/说明 + 行金额展示 | 完成 | `QuoteProjectView.vue` |

## Phase C — 工作区模式（侧栏弱化报价）

| 任务 | 状态 | 说明 |
|------|------|------|
| `aa_tenant.workspace_prefs_json` | 完成 | `quoteNavVisible` 默认 `true` |
| API：`GET/PATCH /api/admin/workspace/prefs` | 完成 | `AdminWorkspaceController` + `TenantBillingService` |
| 侧栏与工作台勾选 | 完成 | `App.vue` + `DashboardView.vue`，保存后广播刷新侧栏 |

## Phase D — 历史数据与运维

| 任务 | 状态 | 说明 |
|------|------|------|
| 存量项目兼容 | 完成 | 迁移默认 `deliverable_key=default`；旧结果无行金额时需重新「计算报价」后再调价 |
| 多租户列幂等 | 完成 | 迁移脚本使用 `information_schema` 条件追加列 |

## 后续可选（未在本分支承诺）

- 按交付物单独目标价（分组系数）  
- 非等比例单行覆盖价  
- 租户级默认「隐藏报价」 onboarding 向导  

---

**分支**：`cursor/quote-solution-scale-workspace-92cd`（完成后合并入 `main`）
